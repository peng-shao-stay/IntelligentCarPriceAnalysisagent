# 分类标签

每个 issue 经过一个状态流转。标签以 YAML frontmatter `labels:` 的形式存储在 issue markdown 文件中。

| 角色 | 标签 | 含义 |
|---|---|---|
| 待评估 | `待评估` | 维护者需要评估这个 issue |
| 等回复 | `等回复` | 等待报告者补充更多信息 |
| AI可处理 | `AI可处理` | 描述完整，AI agent 可直接接手 |
| 需人工 | `需人工` | 需要人类开发者实现 |
| 不做 | `不做` | 不会处理 |

所有标签使用中文名称。
